{
  "name": "bocal-monorepo",
  "private": true,
  "version": "0.0.3",
  "scripts": {
    "build": "pnpm -r --filter \"./packages/*\" build",
    "dev:render-demo": "pnpm --filter @bocal/render-demo dev",
    "docs:dev": "pnpm --filter @bocal/docs dev",
    "docs:build": "pnpm --filter @bocal/docs build",
    "docs:preview": "pnpm --filter @bocal/docs preview"
  },
  "devDependencies": {
    "typescript": "^5.0.2"
  },
  "packageManager": "pnpm@9.0.0"
}
