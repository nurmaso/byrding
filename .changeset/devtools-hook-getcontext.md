---
"@byrding/core": minor
---

Add `getContext()` to `ByrdingDevtoolsHook` interface and the installed `window.__BYRDING_DEVTOOLS__` object. Delegates to core `getContext()`. Available for browser console debugging and Chrome DevTools Extension (#39) without requiring an ES module import.
